# UI/UX 問題清單與改善計劃

## ✅ 已完成

### 1. **頁面回報彈窗置中**

- ✅ 修復 `ReportIssueDialog` 完全置中（垂直 + 水平）
- ✅ 點擊背景可關閉彈窗
- ✅ Dialog 寬度增加至 `max-w-2xl`（更寬敞）

### 2. **頁面回報位置統一**

- ✅ **只保留 Footer 的頁面回報按鈕**
- ✅ 移除 Navbar/UserCenter 的重複按鈕
- ✅ 簡化 UserCenter 組件

### 3. **背景美化**

- ✅ 漸層光暈效果（藍色 + 紫色）
- ✅ 精緻網格紋理

### 4. **Logo 行動版優化**

- ✅ 優先顯示 "Apple MDM Hub" 而非公司名

### 5. **導航翻譯**

- ✅ "首頁" 正確翻譯為 "Home"

### 6. **資源連結補全**

- ✅ 所有 10+ 個資源連結已補齊

---

## 🔄 建議改善項目（排版與審美）

### A. **首頁優化**

1. **Hero Section**:
   - 標題字體大小響應式優化
   - CTA 按鈕間距調整
   - 背景光暈位置微調

2. **卡片排列**:
   - 三欄卡片在平板時改為２欄
   - 卡片 hover 效果增強

### B. **Navbar 優化**

1. **間距調整**:
   - Logo 與選單項目間距增加
   - 行動版 hamburger 按鈕更明顯

2. **Resources 下拉選單**:
   - 寬度增加，避免文字截斷
   - 分組標題樣式強化

### C. **Footer 優化**

1. **ReportIssue 按鈕**:
   - 樣式更醒目（紅色主題）
   - 位置調整至更明顯處

2. **公司資訊排版**:
   - 字體大小層次優化
   - 間距調整

### D. **Guide/Glossary 頁面**

1. **側邊欄**:
   - Font Size Adjustment 只影響內文
   - 字體縮放範圍優化

2. **內容區域**:
   - 響應式間距調整
   - 卡片陰影效果增強

### E. **行動版全局優化**

1. **觸控目標**:
   - 所有按鈕最小 44px
   - 間距增加避免誤觸

2. **文字可讀性**:
   - 最小字體 12px
   - 行高調整

---

## 🛠️ 實作優先順序

**High Priority** ⭐⭐⭐:

- [ ] Footer ReportIssue 按鈕樣式強化
- [ ] Navbar Resources 下拉選單寬度
- [ ] 首頁卡片響應式優化

**Medium Priority** ⭐⭐:

- [ ] 行動版觸控目標擴大
- [ ] Font Size Adjustment 作用範圍修正
- [ ] Guide/Glossary 間距優化

**Low Priority** ⭐:

- [ ] Hero Section 微調
- [ ] Footer 排版精修
- [ ] 全局動畫效果統一

---

## 📝 未登入用戶保護機制說明

### 三層防護架構：

#### 1️⃣ **Middleware 層（服務器端）**

```typescript
// middleware.ts
const isProtectedRoute = createRouteMatcher(["/guide(.*)", "/glossary(.*)"]);

export default clerkMiddleware(async (auth, req) => {
  const authObject = await auth();
  if (isProtectedRoute(req) && !authObject.userId) {
    return authObject.redirectToSignIn(); // 自動導向登入頁
  }
});
```

- **觸發時機**：用戶訪問 `/guide` 或 `/glossary`
- **行為**：自動導向 Clerk 登入頁（`/sign-in`）

#### 2️⃣ **API 層保護**

```typescript
// /api/guide.ts & /api/glossary.ts
const { userId } = getAuth(req);

if (!userId) {
  return res.status(401).json({ error: "Unauthorized" });
}
```

- **觸發時機**：客戶端呼叫 API 獲取數據
- **行為**：返回 `401 Unauthorized`，數據無法獲取

#### 3️⃣ **客戶端空數據處理**

```typescript
// Guide.tsx & Glossary.tsx
const fetchData = async () => {
  const res = await fetch(`/api/guide?lang=${locale}`);
  if (res.ok) {
    const data = await res.json();
    setData(data);
  }
  // 如果 401，data 保持為空陣列
};
```

- **觸發時機**：API 返回 401
- **行為**：頁面顯示空狀態（EmptyState 或載入中）

### 總結

✅ **點進去就會被踢去登入頁**  
✅ **即使繞過 Middleware，API 也會拒絕返回數據**  
✅ **F12 開發者工具也看不到內容（服務器拒絕）**
